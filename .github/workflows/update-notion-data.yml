name: Update Notion Data

on:
  schedule:
    # 3시간마다 실행 (00:00, 03:00, 06:00, 09:00, 12:00, 15:00, 18:00, 21:00 UTC)
    - cron: '0 */3 * * *'
  workflow_dispatch: # 수동 실행 가능

permissions:
  contents: write  # 파일 수정 및 커밋 권한

jobs:
  update-data:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'

    - name: Create data directory
      run: |
        mkdir -p data

    - name: Fetch Notion data
      run: |
        HTTP_STATUS=$(curl -s -w "%{http_code}" -o notion-raw.json -X POST "https://api.notion.com/v1/databases/23787a1932c481bba2c1d5f33256cc37/query" \
          -H "Authorization: Bearer ${{ secrets.NOTION_API_KEY }}" \
          -H "Notion-Version: 2022-06-28" \
          -H "Content-Type: application/json" \
          -d '{
            "page_size": 50
          }')

        echo "HTTP Status: $HTTP_STATUS"

        if [ "$HTTP_STATUS" != "200" ]; then
          echo "❌ Notion API 오류 (HTTP $HTTP_STATUS):"
          cat notion-raw.json
          exit 1
        fi

        echo "✅ Notion API 호출 성공"

    - name: Transform data to calendar format
      run: |
        node -e "
        const fs = require('fs');

        let rawData;
        try {
          rawData = JSON.parse(fs.readFileSync('notion-raw.json', 'utf8'));
        } catch (e) {
          console.error('❌ JSON 파싱 오류:', e.message);
          process.exit(1);
        }

        // API 오류 응답 확인
        if (rawData.object === 'error') {
          console.error('❌ Notion API 오류:', rawData.message);
          console.error('코드:', rawData.code);
          process.exit(1);
        }

        // results 배열 확인
        if (!rawData.results || !Array.isArray(rawData.results)) {
          console.error('❌ 유효하지 않은 응답 형식. results 배열이 없습니다.');
          console.error('응답 내용:', JSON.stringify(rawData, null, 2).substring(0, 500));
          process.exit(1);
        }

        console.log('📊 총 ' + rawData.results.length + '개 기수 발견');

        const calendarData = [];

        rawData.results.forEach((course, index) => {
          const props = course.properties;

          // 디버깅: 사용 가능한 속성 출력
          console.log(\`\\n📋 기수 \${index + 1} 속성 목록:\`);
          Object.keys(props).forEach(key => {
            const prop = props[key];
            if (prop.type === 'date' && prop.date) {
              console.log(\`  - \${key} (date): \${prop.date.start}\`);
            } else if (prop.type === 'title' && prop.title?.[0]) {
              console.log(\`  - \${key} (title): \${prop.title[0].text?.content}\`);
            }
          });

          // 기본 정보 추출
          const title = props.기수명?.title?.[0]?.text?.content || \`강의 \${index + 1}\`;
          const category = props.강의종류?.select?.name || 'SCM 기초';
          const status = props.상태?.select?.name || '준비중';
          const description = props.비고?.rich_text?.[0]?.text?.content || '';
          const price = props.수강료?.number || 0;
          const maxStudents = props.최대인원?.number || 20;
          const currentStudents = props.현재등록인원?.number || 0;

          // 기수 번호 추출 (숫자 필드 또는 타이틀에서 추출)
          let batch = props.기수?.number;
          if (!batch) {
            // 기수 필드가 없으면 기수명에서 숫자 추출
            const batchMatch = title.match(/(\\d+)기/);
            batch = batchMatch ? parseInt(batchMatch[1]) : (index + 1);
          }
          console.log(\`  기수: \${batch}\`);

          // 강의 날짜 추출 (강의(1), 강의(2), 강의(3))
          const lecture1 = props['강의(1)']?.date?.start;
          const lecture2 = props['강의(2)']?.date?.start;
          const lecture3 = props['강의(3)']?.date?.start;

          // 과제세션 날짜 추출 (과제세션(1), 과제세션(2))
          const assignment1 = props['과제세션(1)']?.date?.start;
          const assignment2 = props['과제세션(2)']?.date?.start;

          console.log(\`\\n📅 추출된 날짜:\`);
          console.log(\`  강의(1): \${lecture1 || '없음'}\`);
          console.log(\`  강의(2): \${lecture2 || '없음'}\`);
          console.log(\`  강의(3): \${lecture3 || '없음'}\`);
          console.log(\`  과제세션(1): \${assignment1 || '없음'}\`);
          console.log(\`  과제세션(2): \${assignment2 || '없음'}\`);

          // 삭제되지 않은 기수만 처리
          if (course.archived || course.in_trash) return;

          // 공통 기수 정보
          const courseInfo = {
            id: course.id,
            title,
            category,
            status,
            description,
            price,
            maxStudents,
            currentStudents,
            batch
          };

          // 강의(1) - 1주차: 강의 + 과제
          if (lecture1) {
            calendarData.push({
              name: title,
              date: lecture1,
              type: 'lecture',
              week: 1,
              label: '1주차 강의',
              hasAssignment: true,
              status: status,
              batch: batch,
              maxStudents: maxStudents,
              currentStudents: currentStudents,
              notionData: courseInfo
            });
          }

          // 과제세션(1) - 2주차: 피드백 + 과제
          if (assignment1) {
            calendarData.push({
              name: title,
              date: assignment1,
              type: 'assignment',
              week: 2,
              label: '2주차 피드백',
              hasAssignment: true,
              status: status,
              batch: batch,
              maxStudents: maxStudents,
              currentStudents: currentStudents,
              notionData: courseInfo
            });
          }

          // 강의(2) - 3주차: 강의 + 과제
          if (lecture2) {
            calendarData.push({
              name: title,
              date: lecture2,
              type: 'lecture',
              week: 3,
              label: '3주차 강의',
              hasAssignment: true,
              status: status,
              batch: batch,
              maxStudents: maxStudents,
              currentStudents: currentStudents,
              notionData: courseInfo
            });
          }

          // 과제세션(2) - 4주차: 피드백 + 과제
          if (assignment2) {
            calendarData.push({
              name: title,
              date: assignment2,
              type: 'assignment',
              week: 4,
              label: '4주차 피드백',
              hasAssignment: true,
              status: status,
              batch: batch,
              maxStudents: maxStudents,
              currentStudents: currentStudents,
              notionData: courseInfo
            });
          }

          // 강의(3) - 5주차: 최종 발표 (과제 없음)
          if (lecture3) {
            calendarData.push({
              name: title,
              date: lecture3,
              type: 'lecture',
              week: 5,
              label: '5주차 최종발표',
              hasAssignment: false,
              status: status,
              batch: batch,
              maxStudents: maxStudents,
              currentStudents: currentStudents,
              notionData: courseInfo
            });
          }
        });

        // 날짜순 정렬
        calendarData.sort((a, b) => new Date(a.date) - new Date(b.date));

        const output = {
          success: true,
          data: calendarData,
          lastUpdated: new Date().toISOString(),
          totalSessions: calendarData.length
        };

        fs.writeFileSync('data/schedule.json', JSON.stringify(output, null, 2));
        console.log(\`✅ \${calendarData.length}개 세션 데이터 업데이트 완료\`);
        "

    - name: Fetch Reviews from Notion
      run: |
        HTTP_STATUS=$(curl -s -w "%{http_code}" -o reviews-raw.json -X POST "https://api.notion.com/v1/databases/243951127368492d906a3d36861aacd2/query" \
          -H "Authorization: Bearer ${{ secrets.NOTION_API_KEY }}" \
          -H "Notion-Version: 2022-06-28" \
          -H "Content-Type: application/json" \
          -d '{
            "page_size": 100
          }')

        echo "HTTP Status: $HTTP_STATUS"

        if [ "$HTTP_STATUS" != "200" ]; then
          echo "❌ Notion Reviews API 오류 (HTTP $HTTP_STATUS):"
          cat reviews-raw.json
          exit 1
        fi

        echo "✅ Notion Reviews API 호출 성공"

    - name: Transform reviews data
      run: |
        node -e "
        const fs = require('fs');

        let rawData;
        try {
          rawData = JSON.parse(fs.readFileSync('reviews-raw.json', 'utf8'));
        } catch (e) {
          console.error('❌ Reviews JSON 파싱 오류:', e.message);
          process.exit(1);
        }

        if (rawData.object === 'error') {
          console.error('❌ Notion API 오류:', rawData.message);
          process.exit(1);
        }

        if (!rawData.results || !Array.isArray(rawData.results)) {
          console.error('❌ 유효하지 않은 응답 형식');
          process.exit(1);
        }

        console.log('📊 총 ' + rawData.results.length + '개 후기 발견');

        const reviews = [];

        rawData.results.forEach((review, index) => {
          if (review.archived || review.in_trash) return;

          const props = review.properties;

          // 모든 후기의 속성 확인을 위한 디버깅 강화
          console.log('\\n📋 후기 #' + (index + 1) + ' 속성:');
          Object.keys(props).forEach(key => {
            const prop = props[key];
            let value = '(empty)';
            if (prop.type === 'title' && prop.title?.[0]) {
              value = prop.title[0].text?.content || prop.title[0].plain_text || '';
            } else if (prop.type === 'rich_text' && prop.rich_text?.[0]) {
              value = prop.rich_text[0].text?.content || prop.rich_text[0].plain_text || '';
            } else if (prop.type === 'number') {
              value = prop.number;
            } else if (prop.type === 'select' && prop.select) {
              value = prop.select.name;
            }
            console.log('  - ' + key + ' (' + prop.type + '): ' + value);
          });

          // 후기 제목 추출
          let title = '';
          if (props['후기 제목']?.title?.[0]) {
            title = props['후기 제목'].title[0].text?.content || props['후기 제목'].title[0].plain_text || '';
          }

          // 후기 내용 추출 (다양한 필드명 시도)
          let content = '';
          const contentFields = ['후기 내용', '후기내용', '후기', '내용', 'content', 'Content', 'Review'];
          for (const field of contentFields) {
            const prop = props[field];
            if (prop?.rich_text?.[0]) {
              content = prop.rich_text[0].text?.content || prop.rich_text[0].plain_text || '';
              if (content) break;
            }
            if (prop?.title?.[0]) {
              content = prop.title[0].text?.content || prop.title[0].plain_text || '';
              if (content) break;
            }
          }

          // 제목이 있지만 내용이 없으면 제목을 내용으로 사용
          if (!content && title) {
            content = title;
          }

          // 별점 추출 - 더 많은 필드명 지원
          let rating = 5;
          let ratingFound = false;
          const ratingFields = ['별점', 'rating', 'Rating', '평점', '점수', 'Score', 'Stars', '만족도'];

          // 별점 파싱 함수 (다양한 포맷 지원)
          function parseRating(rawValue) {
            if (!rawValue) return null;

            const str = String(rawValue).trim();
            console.log('    🔍 원본 값: "' + str + '"');

            // 1. 숫자만 있는 경우: "5", "4", "3" 등
            const numOnly = parseInt(str);
            if (!isNaN(numOnly) && numOnly >= 1 && numOnly <= 5) {
              console.log('    ✓ 숫자 파싱 성공: ' + numOnly);
              return numOnly;
            }

            // 2. "N점" 형식: "5점", "4점", "3점" 등
            const matchPoints = str.match(/^(\d+)점?$/);
            if (matchPoints) {
              const num = parseInt(matchPoints[1]);
              if (num >= 1 && num <= 5) {
                console.log('    ✓ "N점" 파싱 성공: ' + num);
                return num;
              }
            }

            // 3. 별 개수 세기: "★★★★★", "⭐⭐⭐⭐" 등
            const starCount = (str.match(/[★⭐]/g) || []).length;
            if (starCount >= 1 && starCount <= 5) {
              console.log('    ✓ 별 개수 파싱 성공: ' + starCount);
              return starCount;
            }

            // 4. 한글 만족도 매핑
            const satisfactionMap = {
              '매우만족': 5, '매우 만족': 5, '최고': 5, '아주좋음': 5,
              '만족': 4, '좋음': 4,
              '보통': 3, '괜찮음': 3,
              '불만족': 2, '아쉬움': 2,
              '매우불만족': 1, '매우 불만족': 1
            };
            const lowerStr = str.toLowerCase();
            for (const [key, value] of Object.entries(satisfactionMap)) {
              if (lowerStr.includes(key.toLowerCase())) {
                console.log('    ✓ 만족도 매핑 성공: ' + key + ' → ' + value);
                return value;
              }
            }

            console.log('    ⚠️ 파싱 실패 - 기본값 5로 설정');
            return null;
          }

          for (const field of ratingFields) {
            if (props[field]?.number) {
              rating = props[field].number;
              console.log('  → 별점 필드 발견 (number): ' + field + ' = ' + rating);
              ratingFound = true;
              break;
            }
            if (props[field]?.select?.name) {
              const ratingStr = props[field].select.name;
              console.log('  → 별점 필드 발견 (select): ' + field);
              const parsed = parseRating(ratingStr);
              if (parsed !== null) {
                rating = parsed;
                ratingFound = true;
                break;
              }
            }
            if (props[field]?.rich_text?.[0]) {
              const ratingStr = props[field].rich_text[0].text?.content || props[field].rich_text[0].plain_text || '';
              if (ratingStr) {
                console.log('  → 별점 필드 발견 (rich_text): ' + field);
                const parsed = parseRating(ratingStr);
                if (parsed !== null) {
                  rating = parsed;
                  ratingFound = true;
                  break;
                }
              }
            }
          }

          if (!ratingFound) {
            console.log('  ⚠️ 별점 필드를 찾지 못함 - 기본값 5 사용');
          }
          console.log('  → 최종 별점: ' + rating);

          // 작성자 추출
          let author = '';
          const authorFields = ['작성자', '이름', 'author', 'Author', 'Name'];
          for (const field of authorFields) {
            const prop = props[field];
            if (prop?.rich_text?.[0]) {
              author = prop.rich_text[0].text?.content || prop.rich_text[0].plain_text || '';
              if (author) break;
            }
            if (prop?.title?.[0]) {
              author = prop.title[0].text?.content || prop.title[0].plain_text || '';
              if (author) break;
            }
          }

          // 날짜 추출
          let date = '';
          const dateFields = ['작성일자', '날짜', '작성일', 'date', 'Date', '등록일'];
          for (const field of dateFields) {
            if (props[field]?.date?.start) {
              const d = new Date(props[field].date.start);
              date = d.getFullYear() + '.' + String(d.getMonth() + 1).padStart(2, '0');
              break;
            }
          }

          // 생성일 fallback
          if (!date && review.created_time) {
            const d = new Date(review.created_time);
            date = d.getFullYear() + '.' + String(d.getMonth() + 1).padStart(2, '0');
          }

          // 카테고리 추출 (컨설팅/부트캠프 구분) - 더 많은 필드명 지원
          let category = 'bootcamp';
          const categoryFields = ['서비스 종류', '서비스종류', '프로그램', '카테고리', 'category', 'Category', '유형', '종류', '타입', 'Type', '서비스', 'Service'];
          for (const field of categoryFields) {
            if (props[field]?.select?.name) {
              const catName = props[field].select.name.toLowerCase();
              console.log('  → 카테고리 필드 발견: ' + field + ' = ' + catName);
              if (catName.includes('컨설팅') || catName.includes('consulting') || catName.includes('1:1') || catName.includes('1대1') || catName.includes('커리어') || catName.includes('career')) {
                category = 'consulting';
              } else if (catName.includes('부트') || catName.includes('bootcamp') || catName.includes('boot') || catName.includes('scm') || catName.includes('직무')) {
                category = 'bootcamp';
              }
              break;
            }
            // rich_text 타입도 확인
            if (props[field]?.rich_text?.[0]) {
              const catName = (props[field].rich_text[0].text?.content || props[field].rich_text[0].plain_text || '').toLowerCase();
              if (catName) {
                console.log('  → 카테고리 필드 발견 (rich_text): ' + field + ' = ' + catName);
                if (catName.includes('컨설팅') || catName.includes('consulting') || catName.includes('1:1') || catName.includes('1대1') || catName.includes('커리어') || catName.includes('career')) {
                  category = 'consulting';
                } else if (catName.includes('부트') || catName.includes('bootcamp') || catName.includes('boot') || catName.includes('scm') || catName.includes('직무')) {
                  category = 'bootcamp';
                }
                break;
              }
            }
          }
          console.log('  → 최종 카테고리: ' + category);

          if (content) {
            reviews.push({
              id: review.id,
              title: title,
              content: content,
              rating: rating,
              author: author,
              date: date,
              category: category
            });
            console.log('✓ 후기 추가: ' + author + ' (' + rating + '점, ' + category + ')');
          } else {
            console.log('✗ 후기 제외 (내용 없음): ID=' + review.id + ', 카테고리=' + category + ', 작성자=' + (author || '(없음)') + ', 별점=' + rating);
            console.log('  ℹ️  "후기 내용" 필드를 확인하세요. 제목만 있고 내용이 비어있을 수 있습니다.');
          }
        });

        // 날짜 역순 정렬 (최신순)
        reviews.sort((a, b) => {
          if (!a.date) return 1;
          if (!b.date) return -1;
          return b.date.localeCompare(a.date);
        });

        // 통계 분석
        console.log('\n━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━');
        console.log('📊 후기 데이터 통계');
        console.log('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n');

        const categoryStats = {};
        const ratingStats = {};
        reviews.forEach(r => {
          categoryStats[r.category] = (categoryStats[r.category] || 0) + 1;
          ratingStats[r.rating] = (ratingStats[r.rating] || 0) + 1;
        });

        console.log('카테고리별 분포:');
        Object.entries(categoryStats).forEach(([cat, count]) => {
          console.log('  - ' + cat + ': ' + count + '개');
        });

        console.log('\n평점별 분포:');
        Object.entries(ratingStats).sort((a, b) => b[0] - a[0]).forEach(([rating, count]) => {
          console.log('  - ' + rating + '점: ' + count + '개');
        });

        const lowRatingCount = reviews.filter(r => r.rating < 5).length;
        console.log('\n✓ 총 후기: ' + reviews.length + '개');
        console.log('✓ 1대1컨설팅: ' + (categoryStats.consulting || 0) + '개');
        console.log('✓ 4점 이하: ' + lowRatingCount + '개');

        if (!categoryStats.consulting || categoryStats.consulting === 0) {
          console.log('\n⚠️  경고: 1대1컨설팅 후기가 없습니다!');
          console.log('   → Notion의 "프로그램" 필드 확인 필요');
        }

        if (lowRatingCount === 0) {
          console.log('\n⚠️  경고: 4점 이하 후기가 없습니다!');
          console.log('   → Notion의 "별점" 필드 값 확인 필요');
          console.log('   → 위 로그에서 "🔍 원본 값" 확인');
        }

        console.log('\n━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n');

        const output = {
          success: true,
          data: reviews,
          lastUpdated: new Date().toISOString(),
          totalReviews: reviews.length
        };

        fs.writeFileSync('data/reviews.json', JSON.stringify(output, null, 2));
        console.log('✅ ' + reviews.length + '개 후기 데이터 업데이트 완료');
        "

    - name: Commit and push if changed
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add data/schedule.json data/reviews.json
        if ! git diff --cached --exit-code; then
          git commit -m "🔄 Notion 데이터 자동 업데이트 $(date '+%Y-%m-%d %H:%M')"
          git push
        else
          echo "데이터 변경사항 없음"
        fi
