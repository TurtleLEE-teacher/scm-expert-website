{
  "name": "scm-expert-website",
  "version": "1.0.0",
  "description": "SCM Labs Website with Vercel Functions",
  "main": "index.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step required for static site'",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "vercel": "^28.16.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}